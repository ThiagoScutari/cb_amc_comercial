# Runtime — versões pinadas (espelham o ambiente de dev p/ CI reproduzível).
# Camadas: web (FastAPI), LLM (Claude), STT (OpenAI/Whisper), dados (SQLAlchemy+psycopg3).
fastapi==0.136.1
uvicorn[standard]==0.34.0
anthropic==0.96.0
openai==2.32.0
elevenlabs==2.53.0      # TTS de saída (voz PT-BR); client real montado na factory da Fase 8
pydantic==2.10.3
pydantic-settings==2.14.2      # 2.14.0 tinha GHSA-4xgf-cpjx-pc3j (pip-audit) — bump p/ 2.14.2
sqlalchemy==2.0.36
psycopg[binary]==3.2.3          # driver Postgres (psycopg3, uso síncrono)
httpx==0.28.1
jinja2==3.1.6           # 3.1.4 tinha CVE-2024-56326/56201/2025-27516 (pip-audit) — bump p/ 3.1.6

# Itens adiados (entram na fase correspondente, p/ evitar peso/CVE prematuro):
#   python-multipart  -> Fase 8 (upload no webhook Evolution)
