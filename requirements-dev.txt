# Ferramentas de dev/CI — não vão para a imagem de runtime.
# Instalar com: pip install -r requirements-dev.txt
-r requirements.txt

ruff==0.15.11            # lint + format (RuboCop -> ruff)
bandit==1.9.4            # SAST (Brakeman -> bandit)
pip-audit==2.7.3         # CVE em dependências (bundler-audit -> pip-audit)
mypy==1.15.0             # tipagem estática
pytest==8.3.4
pytest-asyncio==1.3.0
pytest-cov==7.1.0        # cobertura (SimpleCov -> pytest-cov)
testcontainers==4.14.2   # Postgres efêmero p/ testes de IDOR (@pytest.mark.postgres)
